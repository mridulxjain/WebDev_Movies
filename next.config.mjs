/** @type {import('next').NextConfig} */
// Next.js config: tweak framework-level settings here.
// TODO: Configure `images.domains` if fetching remote posters, and experimental flags if needed.
const nextConfig = {
  reactStrictMode: true,
};

export default nextConfig;
